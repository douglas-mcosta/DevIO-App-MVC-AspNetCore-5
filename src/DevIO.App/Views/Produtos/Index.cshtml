@model IEnumerable<DevIO.App.DTO.ProdutoDTO>

@{
    ViewData["Title"] = "Lista de Produtos";
}

<h4 class="pt-3">@ViewData["Title"]</h4>
<hr />
<p>
    <a class="btn btn-info" asp-action="Create">Novo Produto</a>
</p>
<div>
    <partial name="_ListaProdutos" />
</div>